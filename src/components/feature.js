import React, { Component } from 'react';

export default class Feature extends Component {
    render() {
        return(
            <h1>Super Secret page for registered users</h1>
        )
    }
}